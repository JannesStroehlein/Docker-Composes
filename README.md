# Docker Composes

This repository contains Docker Compose files for various applications and services I self host. Each directory corresponds to a specific application or service, and contains the necessary configuration files to set up and run the application using Docker Compose.
